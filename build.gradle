buildscript {
    repositories {
        mavenLocal()
    }
    dependencies {
        classpath 'org.example:greeting-plugin:1.0'
    }
}
apply plugin: 'org.example.greeting'

greeting {
    message = 'hey'
    greeter = 'bro'
}

group 'org.example'
version '1.0-SNAPSHOT'