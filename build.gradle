buildscript {
    repositories {
        mavenLocal()
    }
    dependencies {
        classpath 'org.example:greeting-plugin:1.2'
    }
}
apply plugin: 'org.example.greeting'

greeting {
    message = 'Hello'
    greeter = 'Gradle'
}

group 'org.example'
version '1.0-SNAPSHOT'